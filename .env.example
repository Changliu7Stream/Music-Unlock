# MusicUnlock 后端解密服务 - 环境变量配置
#
# 复制此文件为 .env 并填入实际密钥值：
#   cp .env.example .env
#
# 所有密钥均为各加密格式规范中的公开常量，可从 unlock-music / parakeet-crypto-rs
# 等开源项目获取。本文件不包含任何实际密钥值，仅作模板说明。
#
# 启动时 decrypt_algorithms.py 会打印各密钥的加载状态（不输出实际值）。

# === NCM (网易云音乐) ===
# NCM 核心密钥，用于解密文件内嵌的主密钥（字符串）
NCM_CORE_KEY=<填入 NCM 核心密钥字符串>
# NCM 标签密钥，用于解密歌曲信息（可选，留空则使用库默认值）
# NCM_TAG_KEY=<可选，留空使用库默认值>

# === QMCv2 (QQ音乐新版 mflac/mgg) ===
# 方式一：直接提供 core_key（hex 编码，8 字节 = 16 hex 字符）
# QMCV2_CORE_KEY=<填入 16 位 hex 字符串>
# 方式二：通过 salt+length 派生（二选一，优先使用方式一）
QMCV2_CORE_KEY_SALT=<填入 salt 整数，库默认 106>
QMCV2_CORE_KEY_LENGTH=<填入 length 整数，库默认 8>

# === QMCv1 (QQ音乐旧版 qmc*) ===
# QMCv1 mask，256 字节（hex 编码 = 512 hex 字符）
# 库会自动通过 from_qmcv1_mask256() 转换为 128 字节 mask
QMCV1_MASK=<填入 512 位 hex 字符串>

# === KGM/VPR (酷狗音乐) ===
# KGM 解码表1，272 字节（hex 编码 = 544 hex 字符）
KGM_TABLE1=<填入 544 位 hex 字符串>
# KGM 解码表2，272 字节（hex 编码 = 544 hex 字符）
KGM_TABLE2=<填入 544 位 hex 字符串>
# KGM 解码表v2 (MaskV2PreDef)，272 字节（hex 编码 = 544 hex 字符）
KGM_TABLEV2=<填入 544 位 hex 字符串>
# VPR 密钥，17 字节（仅解密 .vpr 文件时需要）
VPR_KEY=<填入 VPR 密钥 hex 字符串>

# === KWM (酷我音乐) ===
# KWM 核心密钥（字符串）
KWM_CORE_KEY=<填入 KWM 核心密钥字符串>
